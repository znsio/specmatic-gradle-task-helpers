buildscript {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }

    dependencies {
        classpath 'net.researchgate:gradle-release:3.1.0'
    }
}

rootProject.pluginManager.apply(net.researchgate.release.ReleasePlugin)

